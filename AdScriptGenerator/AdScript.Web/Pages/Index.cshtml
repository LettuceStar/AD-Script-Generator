@page
@model AdScript.Web.Pages.IndexModel


<details>
    <summary>Single user (quick test)</summary>

    <form method="post">

    <div asp-validation-summary="All" style="color: red; margin-bottom: 12px;"></div>

    <div>
        <label>First Name</label><br />
        <input asp-for="Input.FirstName" />
        <span asp-validation-for="Input.FirstName" style="color: red;"></span>
    </div>
    <br />

    <div>
        <label>Last Name</label><br />
        <input asp-for="Input.LastName" />
        <span asp-validation-for="Input.LastName" style="color: red;"></span>
    </div>
    <br />

    <div>
        <label>Employee ID</label><br />
        <input asp-for="Input.ID" />
        <span asp-validation-for="Input.ID" style="color: red;"></span>
    </div>
    <br />

    <div>
        <label>Campus</label><br />
        <input asp-for="Input.Campus" />
        <span asp-validation-for="Input.Campus" style="color: red;"></span>
    </div>
    <br />

    <div>
        <label>Team</label><br />
        <input asp-for="Input.Team" />
        <span asp-validation-for="Input.Team" style="color: red;"></span>
    </div>
    <br />

    <div>
        <label>UPN Suffix</label><br />
        <input asp-for="Input.UpnSuffix" placeholder="@@cats.local" />
        <span asp-validation-for="Input.UpnSuffix" style="color: red;"></span>
    </div>
    <br />

    <button type="submit" asp-page-handler="Generate">
        Generate
    </button>


    </form>

</details>




@if (!string.IsNullOrWhiteSpace(Model.GeneratedCommand))
{
    <hr />
    <h3>Generated Command</h3>

    <pre id="cmd" style="white-space: pre-wrap; padding: 12px; border: 1px solid #ccc;">
        @Model.GeneratedCommand
    </pre>

    <button type="button" onclick="copyCommand()" id="copyBtn">
        Copy
    </button>

    <span id="copyMessage" style="color: green; margin-left: 10px; display: none;">
        Copied!
    </span>

    <script>
        function copyCommand() {
            const text = document.getElementById('cmd').innerText;

            navigator.clipboard.writeText(text).then(function () {
                const msg = document.getElementById('copyMessage');
                msg.style.display = "inline";

                setTimeout(function () {
                    msg.style.display = "none";
                }, 3000);
            });
        }
    </script>

}


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


<hr />

<h2>Bulk (Excel)</h2>

<form method="post" enctype="multipart/form-data" asp-page-handler="Upload">
    <div>
        <label>Account Type</label>
        <select asp-for="UploadAccountType">
            <option value="Staff">Staff</option>
            <option value="Student">Student</option>
        </select>
    </div>

    <div>
        <input type="file" asp-for="UploadFile" accept=".xlsx" />
        <button type="submit">Upload</button>
    </div>
</form>

@if (Model.TotalRows > 0)
{
        <h3>Upload Summary</h3>

        <div class="stats-grid">
            <div class="stat-card total">
                <div class="stat-label">Total Rows</div>
                <div class="stat-value">@Model.TotalRows</div>
            </div>

            <div class="stat-card valid">
                <div class="stat-label">Valid Rows</div>
                <div class="stat-value">@Model.ValidRowCount</div>
            </div>

            <div class="stat-card invalid @(Model.ErrorCount == 0 ? "invalid-zero" : "")">
                <div class="stat-label">Invalid Rows</div>
                <div class="stat-value">@Model.ErrorCount</div>
            </div>

                <div class="stat-card commands">
                <div class="stat-label">Commands Generated</div>
                <div class="stat-value">@Model.Commands.Count</div>
            </div>
        </div>
}

@if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
{
    <div style="color:red; margin-top:12px;">@Model.ErrorMessage</div>
}

@if (Model.TotalRows > 0)
{
    <div class="section-block">
        <h3 class="section-title">Preview (Top @Model.PreviewRows.Count Valid Rows)</h3>

        <div class="table-wrapper">
            <table class="result-table">
                <thead>
                    <tr>
                        <th>FirstName</th>
                        <th>LastName</th>
                        <th>ID</th>
                        <th>Campus</th>
                        <th>Team</th>
                        <th>UpnSuffix</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var r in Model.PreviewRows)
                    {
                        <tr>
                            <td>@r.FirstName</td>
                            <td>@r.LastName</td>
                            <td>@r.ID</td>
                            <td>@r.Campus</td>
                            <td>@r.Team</td>
                            <td>@r.UpnSuffix</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}

@if (Model.ErrorCount > 0)
{
        <div class="section-block">
            <h3 class="section-title error-title">Validation Errors</h3>
            <p class="error-subtitle">Showing first 50 errors.</p>

            <div class="table-wrapper">
                <table class="result-table">
                    <thead>
                        <tr>
                            <th>Row</th>
                            <th>Field</th>
                            <th>Message</th>
                        </tr>
                    </thead>
                    <tbody>
                    @foreach (var e in Model.RowErrors.Take(50))
                    {
                                <tr>
                                    <td>@e.RowNumber</td>
                                    <td>@e.Field</td>
                                    <td>@e.Message</td>
                                </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
}

@if (Model.Commands?.Count > 0)
{
    <hr />
 
    <h3>Generated Script (@Model.Commands.Count)</h3>
    <pre id="allCmd" class="script-box">@Model.CombinedScript</pre>

    <button type="button" onclick="copyAll()" id="copyAllBtn">Copy all</button>
    <span id="copyAllMessage" class="copy-message">Copied!</span>

     @if (Model.ErrorCount > 0)
    {
        <div class="download-warning">
            Some rows have validation errors. Valid commands can still be downloaded.
        </div>
    }

    <form method="post" asp-page-handler="Download" style="margin-top:12px;">
        <textarea name="CombinedScript" hidden>@Model.CombinedScript</textarea>
        <input type="hidden" name="DownloadFileName" value="@Model.DownloadFileName" />
        <button type="submit">Download .ps1</button>
    </form>


    <script>
        function copyAll() {
            const text = document.getElementById('allCmd').innerText;

            navigator.clipboard.writeText(text).then(function () {
                const msg = document.getElementById('copyAllMessage');
                msg.style.display = 'inline';

                setTimeout(function () {
                    msg.style.display = 'none';
                }, 3000);
            });
        }
    </script>

}

